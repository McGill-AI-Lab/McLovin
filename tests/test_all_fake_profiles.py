import pytest
from src.core.profile import UserProfile, Grade, Ethnicity, Faculty
from src.core.embedder import Embedder  # Replace with the actual module name

def add_all_fake_profiles_in_pinecone():
    """We have generate around """
